<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <!-- <%= task.title %> -->
      <%= link_to task.title, task_path(task) %>
      <!-- - <%= task.details %> -->
      <!-- <%= link_to 'Edit', edit_task_path(task) %> |
      <%= link_to 'Delete', destroy_task_path(task), method: :delete, data: { confirm: 'Are you sure you want to delete this task?' } %> -->
    </li>
  <% end %>
</ul>

<p>
  <%= link_to 'Add a new task', new_task_path %>
</p>
